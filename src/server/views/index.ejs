<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="<%= defaultPageProps.assetsPath %>/main.css" />
    <title>Tecido</title>
    
    <!-- This next line is what actually makes sure we can send information from backend to frontend -->
    <script>window.__APP_CONTEXT__ = JSON.parse(JSON.parse(decodeURI("<%- encodeURI(JSON.stringify(context)); %>")));</script>
</head>
<body>
    <!-- The body will initially be the server-side rendered version of the app, which is later hydrated -->
    <div id="<%= defaultPageProps.containerId %>"><%- body %></div>
    <script src="https://unpkg.com/react@<%= defaultPageProps.reactVersion %>/umd/react<%= defaultPageProps.suffix %>"></script>
    <script src="https://unpkg.com/react-dom@<%= defaultPageProps.reactDomVersion %>/umd/react-dom<%= defaultPageProps.suffix %>"></script>
    <script src="<%= defaultPageProps.assetsPath %>/bundle.js"></script>
</body>
</html>